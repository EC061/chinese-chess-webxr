{
  "name": "chinese-chess-webxr",
  "private": true,
  "version": "1.0.0",
  "description": "Xiangqi (Chinese Chess) as a seated WebXR experience for Meta Quest 3 / 3S — play a client-side AI or other humans in passcode-protected rooms, with rated play and an interactive piece tutorial.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "packages/shared",
    "packages/ai",
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:client",
    "dev:server": "npm run dev -w @ccx/server",
    "dev:client": "npm run dev -w @ccx/client",
    "build": "npm run build -w @ccx/shared && npm run build -w @ccx/ai && npm run build -w @ccx/client && npm run build -w @ccx/server",
    "test": "vitest run",
    "typecheck": "tsc -b packages/shared packages/ai packages/server && tsc -p packages/client --noEmit",
    "start": "node packages/server/dist/server.mjs",
    "bundle": "npm run build && npm run bundle -w @ccx/server"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "esbuild": "^0.28.2",
    "npm-run-all2": "^9.0.3",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
